-- ============================================================================
-- 全時博士生助學金「申請類別」改為可複選（競爭型 + 指導教授配合款）
-- ----------------------------------------------------------------------------
-- 儲存格式沿用 repo 既有的多值字串慣例（lib/scholarship-form-options.ts 的
-- MULTI_OPTION_DELIMITER），多個值以「、」串接，例如：
--
--   競爭型、指導教授配合款
--
-- 因此欄位型別維持 text，不需要改型別也不需要回填：
-- - 既有單選資料本身就是合法的「單一元素」值。
-- - application_summary view 的 payload -> 'applicantInfo' ->> 'applicationType'
--   仍然取得同一個字串。
-- - PDF 匯出與後台顯示直接顯示該字串即可。
--
-- 本腳本只更新欄位註解，可安全重複執行。
-- ============================================================================

comment on column public.scholarship_applications.application_type is
  '申請類別；全時博士生助學金可複選，多值以「、」分隔（例：競爭型、指導教授配合款）';
